(executables
 ((names (main))
  (libraries (
              core_kernel
              js_of_ocaml
              virtual_dom
              incr_dom
              splay_tree
              async_kernel
             ))
  (js_of_ocaml ())
  (preprocess (((pps (ppx_js_driver JANE_KERNEL -ite-check)) All)))
 ))
