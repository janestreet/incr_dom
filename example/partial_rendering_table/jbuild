(executables
 ((names (main))
  (flags (:standard -safe-string))
  (libraries (
    incr_dom
    incr_dom_widgets
    ))
  (preprocess (pps (js_of_ocaml-ppx ppx_jane ppx_driver.runner)))
  ))


(jbuild_version 1)
