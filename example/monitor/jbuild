(executables
 ((names (main))
  (libraries (core_kernel
              js_of_ocaml
              virtual_dom
              incr_dom
              async_kernel
             ))
  (js_of_ocaml ())
  (preprocess (pps (js_of_ocaml.ppx.driver ppx_jane.kernel -ite-check)))
 ))
